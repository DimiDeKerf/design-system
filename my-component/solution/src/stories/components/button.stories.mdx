import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { html } from 'lit-html';

<Meta
  title="Button"
  compoennt="my-button"
  argTypes={{
    label: {
      control: {
        type: 'text'
      },
      description: 'Specify the label of the button'
    },
    size: {
      control: {
        type: 'select'
      },
      options: ['small', 'default', 'large'],
      description: 'Specify the size of the button',
      table: {
        defaultValue: {
          summary: 'default'
        }
      }
    },
    disabled: {
      control: { type: 'boolean' },
      description: 'Specify whether the component should be disabled',
      table: {
        defaultValue: {
          summary: false
        },
      }
    },
    leadingIcon: {
      control: {
        type: 'select'
      },
      options: ['add', 'clear', 'inbox'],
      description: 'Specify the leading icon of the button',
    },
    trailingIcon: {
      control: {
        type: 'select'
      },
      options: ['add', 'clear', 'inbox'],
      description: 'Specify the trailing icon of the button',
    }
  }}
/>

export const Template = ({label, size, disabled, leadingIcon, trailingIcon}) => html ` <my-button .label=${label} .size=${size} .disabled=${disabled} .leadingIcon=${leadingIcon} .trailingIcon=${trailingIcon} /> `

# Button

This is a button

<Canvas>
  <Story
    name="Small"
    args={{
      label: 'Button',
      size: 'small'
    }}
    parameters={{
      docs: {
        source: {
          code: '<my-button label="Button" size="small"></my-button>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Default"
    args={{
      label: 'Button',
      size: 'default'
    }}
    parameters={{
      docs: {
        source: {
          code: '<my-button label="Button" size="default"></my-button>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Large"
    args={{
      label: 'Button',
      size: 'large'
    }}
    parameters={{
      docs: {
        source: {
          code: '<my-button label="Button" size="large"></my-button>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Properties

<ArgsTable story="Default" />

## Disabled

<Canvas>
  <Story
    name="Small disabled"
    args={{
      label: 'Button',
      size: 'small',
      disabled: true
    }}
    parameters={{
      docs: {
        source: {
          code: '<my-button label="Button" size="small" disabled></my-button>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Default disabled"
    args={{
      label: 'Button',
      size: 'default',
      disabled: true
    }}
    parameters={{
      docs: {
        source: {
          code: '<my-button label="Button" size="default" disabled></my-button>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Large disabled"
    args={{
      label: 'Button',
      size: 'large',
      disabled: true
    }}
    parameters={{
      docs: {
        source: {
          code: '<my-button label="Button" size="large" disabled></my-button>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Leading icon

<Canvas>
  <Story
    name="Leading icon"
    args={{
      label: 'Button',
      leadingIcon: 'add'
    }}
    parameters={{
      docs: {
        source: {
          code: '<my-button label="Button" leading-icon="add"></my-button>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Trailing icon

<Canvas>
  <Story
    name="Trailing icon"
    args={{
      label: 'Button',
      trailingIcon: 'add'
    }}
    parameters={{
      docs: {
        source: {
          code: '<my-button label="Button" trailing-icon="add"></my-button>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Icon only

<Canvas>
  <Story
    name="Icon only"
    args={{
      trailingIcon: 'add'
    }}
    parameters={{
      docs: {
        source: {
          code: '<my-button trailing-icon="add"></my-button>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
